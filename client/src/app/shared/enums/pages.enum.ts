export enum Pages {
  Home = 'home',
  NotFound = 'not-found',
  Catalog = 'catalog',
  // Not sure if I have time
  // Profile = 'profile',
  Create = 'create',
  Cart = 'cart',
  Login = 'login',
  SignUp = 'signup',
  Logout = 'logout',
}
