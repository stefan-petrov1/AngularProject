export enum Pages {
  Home = 'home',
  NotFound = 'not-found',
  Catalog = 'catalog',
  Details = 'catalog/:id',
  Cart = 'cart',
  // Not sure if I have time
  // Profile = 'profile',
  Create = 'create',
  Login = 'login',
  SignUp = 'signup',
  Logout = 'logout',
}
